#include<bits/stdc++.h>
int uniqueElement(vector<int> arr, int n)
{
	// Write your code here
    int ans;
    for(int i=0;i<n;i++)
    {
        if(arr[i]!=arr[i+1])
        {
            ans = arr[i];
            break;
        }
        i++;
    }
    return ans;
}

